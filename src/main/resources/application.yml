server:
  port: 8087

management:
  endpoint:
    health:
      show-details: always
  server:
    port: 8088

springdoc:
  swagger-ui:
    enabled: true
    use-root-path: true

axiell:
  url: ${COLLECTIONS_PATH_STAGE}
 