spring:
  security:
    user:
      name: ${PROD_BIKUBE_USERNAME}
      password: ${PROD_BIKUBE_PASSWORD}

security:
  enabled: true

collections:
  url: ${COLLECTIONS_PATH_STAGE}
  username: ${COLLECTIONS_USERNAME_PROD}
  password: ${COLLECTIONS_PASSWORD_PROD}